import { View, Text } from 'react-native'
import React from 'react'

export default function ArrayProps({subject}:{subject:string[]}) {
  return (
    <View>
      <Text>ArrayProps</Text>
    </View>
  )
}